# MathAssistant backend — production/runtime dependencies
# Python 3.11+ (see render.yaml)

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Math engine
sympy>=1.12

# PostgreSQL
sqlalchemy>=2.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# Schema migrations (alembic upgrade head)
alembic>=1.13.0

# Error monitoring (optional — initialized when SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0.0,<3.0.0

httpx
# Test dependencies
pytest>=7.0.0